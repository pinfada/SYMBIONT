# Configuration du serveur de signaling SYMBIONT
# Copier ce fichier en .env et ajuster les valeurs

# Environnement (development ou production)
NODE_ENV=development

# Port du serveur (Render fournira automatiquement la variable PORT)
PORT=8080

# Origines autorisées (séparées par des virgules)
# chrome-extension:// acceptera toutes les extensions Chrome
ALLOWED_ORIGINS=chrome-extension://,http://localhost:3000,https://symbiont.example.com

# Options de sécurité
MAX_MESSAGE_SIZE=65536
CONNECTION_TIMEOUT=120000
HEARTBEAT_INTERVAL=30000