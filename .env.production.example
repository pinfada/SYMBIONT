# Variables d'environnement SYMBIONT - Configuration Production
# IMPORTANT: Copier vers .env et remplir avec des valeurs sécurisées

# ============================================================================
# SÉCURITÉ CRITIQUE - OBLIGATOIRE EN PRODUCTION
# ============================================================================

# JWT Secrets (minimum 64 caractères cryptographiquement sécurisés)
# Générer avec: openssl rand -base64 64
JWT_SECRET=YOUR_SECURE_JWT_SECRET_HERE_64_CHARS_MINIMUM
JWT_REFRESH_SECRET=YOUR_SECURE_REFRESH_SECRET_HERE_64_CHARS_MINIMUM

# API Keys (minimum 32 caractères cryptographiquement sécurisés)
# Générer avec: openssl rand -base64 32
SYMBIONT_API_KEY=YOUR_SECURE_API_KEY_HERE_32_CHARS_MINIMUM
ADMIN_API_KEY=YOUR_SECURE_ADMIN_KEY_HERE_32_CHARS_MINIMUM

# Clé de chiffrement local (AES-256, 32 bytes en base64)
# Générer avec: openssl rand -base64 32
ENCRYPTION_KEY=YOUR_SECURE_ENCRYPTION_KEY_HERE_32_BYTES_BASE64

# ============================================================================
# CONFIGURATION APPLICATION
# ============================================================================

# Environnement (development|staging|production)
NODE_ENV=production

# URLs API
SYMBIONT_API_URL=https://api.symbiont.app
SYMBIONT_WS_URL=wss://api.symbiont.app

# Base de données
DATABASE_URL=postgresql://user:password@localhost:5432/symbiont_production
MONGO_URL=mongodb://localhost:27017
DB_NAME=symbiont_production

# ============================================================================
# CONFIGURATION SERVEUR
# ============================================================================

# Ports
PORT=3001
ADMIN_PORT=8090

# Sécurité CORS
CORS_ORIGIN=https://app.symbiont.com
ALLOWED_HOSTS=app.symbiont.com,api.symbiont.app

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# OBSERVABILITÉ ET MONITORING
# ============================================================================

# Logging
LOG_LEVEL=warn
LOG_FORMAT=json
LOG_RETENTION_DAYS=30

# Métriques
ENABLE_METRICS=true
METRICS_ENDPOINT=/metrics
METRICS_AUTH_TOKEN=YOUR_METRICS_TOKEN

# Tracing (optionnel)
ENABLE_TRACING=false
JAEGER_ENDPOINT=http://localhost:14268/api/traces
TRACE_SAMPLE_RATE=0.1

# ============================================================================
# FONCTIONNALITÉS (Feature Flags)
# ============================================================================

# Activation des vraies données
USE_REAL_METRICS=true
USE_REAL_DNA=true
USE_REAL_BEHAVIOR=true
USE_BACKEND_API=true

# Debug et développement
ENABLE_DEBUG_LOGGING=false
ENABLE_PERFORMANCE_PROFILING=false

# ============================================================================
# COMMANDES DE GÉNÉRATION DES SECRETS
# ============================================================================

# Pour générer des secrets sécurisés, exécuter dans le terminal:
#
# JWT_SECRET=$(openssl rand -base64 64)
# JWT_REFRESH_SECRET=$(openssl rand -base64 64)
# SYMBIONT_API_KEY=$(openssl rand -base64 32)
# ADMIN_API_KEY=$(openssl rand -base64 32)
# ENCRYPTION_KEY=$(openssl rand -base64 32)
#
# Puis remplacer les valeurs ci-dessus